import { Module } from '@nestjs/common';
import { CourseService } from './course.service';

@Module({
    imports: [

    ],
    controllers: [

    ],
    providers: [
        CourseService,
    ],
})
export class CourseModule {}
